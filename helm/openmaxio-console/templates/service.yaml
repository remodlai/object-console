apiVersion: v1
kind: Service
metadata:
  name: {{ include "openmaxio-console.fullname" . }}
  labels:
    {{- include "openmaxio-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "openmaxio-console.selectorLabels" . | nindent 4 }}
